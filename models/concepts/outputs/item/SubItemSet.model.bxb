structure (SubItemSet) {
  description (하위 아이템의 정보를 위한 구조체)
  property (subitems) {
    type (SubItems)
    min (Optional) max (One)
  }

  property (subitemnum) {
    type (SubItemsNum)
    min (Optional) max (One)
  }

  property (itemprice) {
    type (ItemPrice)
    min (Optional) max (One)
  }
}
