structure (ItemSet) {
  description (아이템이름과 API활용을 위한 아이템의 고유값과 하위 아이템 목록을 전달한다.)
  property (items) {
    type (ItemsOut)
    min (Required) max (One)
  }

  property (itemnums) {
    type (ItemsNum)
    min (Required) max (One)
  }

  property (subitemset){
    type (SubItemSet)
    min (Required) max (Many)
  }
}
