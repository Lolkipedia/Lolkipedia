structure (InfoSkillSet) {
  description (인포창을 위한 스킬요약창)

  property (skills) {
    type (SkillsStructure)
    min (Required) max (Many)
  }

  property (skillset) {
    type (SkillSetStructure)
    min (Optional) max (Many)
  }

  property (spells) {
    type (SpellStructure)
    min (Optional) max (Many)
  }
}
