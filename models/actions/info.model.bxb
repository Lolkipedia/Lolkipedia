action (info) {
  description (action for info)
  collect {
    input (name) {
      type (Name)
      min (Required) max (One)
    }

    input (inform) {
      type (Inform)
      min (Optional) max (One)
    }
  }
  output (InfoOutStructure)
}
