action (skills) {
  description (action for skills)
  collect {
    input (name) {
      type (Name)
      min (Required) max (One)
    }

    input (skill) {
      type (Skills)
      min (Required) max (One)
    }
  }
  output (SkillsOutStructure)
}
