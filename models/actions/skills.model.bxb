action (skills) {
  description (챔피언이 활용하기 좋은 스킬을 찾는다.)
  collect {
    input (name) {
      type (Name)
      min (Required) max (One)
    }

    input (skill) {
      type (Skills)
      min (Required) max (One)
    }
  }
  output (SkillsOutStructure)

  type(Search)
}
