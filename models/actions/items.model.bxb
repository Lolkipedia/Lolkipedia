action (items) {
  description (챔피언에게 추천할 만한 아이템을 찾는다.)
  collect {
    input (name) {
      type (Name)
      min (Required) max (One)
    }

    input (item) {
      type (Items)
      min (Required) max (One)
    }
  }
  output (ItemsOutStructure)

  type(Search)
}
