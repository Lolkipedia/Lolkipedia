action (items) {
  description (action for items)
  collect {
    input (name) {
      type (Name)
      min (Required) max (One)
    }

    input (item) {
      type (Items)
      min (Required) max (One)
    }
  }
  output (ItemsOutStructure)
}
