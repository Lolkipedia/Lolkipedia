action (lunes) {
  description (action for lune)
  collect {
    input (name) {
      type (Name)
      min (Required) max (One)
    }

    input (lune) {
      type (Lunes)
      min (Required) max (One)
    }
  }
  output (LunesOutStructure)
}
