action (runes) {
  description (action for lune)
  collect {
    input (name) {
      type (Name)
      min (Required) max (One)
    }

    input (rune) {
      type (Runes)
      min (Required) max (One)
    }
  }
  output (RunesOutStructure)
}
