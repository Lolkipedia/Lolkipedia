action (runes) {
  description (챔피언에게 가장 추천할 수 있는 룬정보를 찾는다.)
  collect {
    input (name) {
      type (Name)
      min (Required) max (One)
    }

    input (rune) {
      type (Runes)
      min (Required) max (One)
    }
  }
  output (RunesOutStructure)

  type(Search)
}
