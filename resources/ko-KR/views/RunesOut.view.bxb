result-view {
  match: RunesOutStructure (this)
  message {
    template("#{value(this.name)}의 추천 룬")
  }
  render {
    layout-macro (runes-Info){
      param(name) {
        expression (name)
      }
      param(runeset) {
        expression (runeset)
      }
      param(statmodset) {
        expression (statmodset)
      }
      param(engname) {
        expression (engname)
      }
      param(version) {
        expression (version)
      }
    }
  }
}