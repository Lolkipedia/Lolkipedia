result-view {
  match: SkillsOutStructure (this) {
  }

  render {
    layout-macro (skill-Tree) {
      param(name) {
        expression (name)
      }
      param(skills) {
        expression (skills)
      }
      param(engname) {
        expression (engname)
      }
      param(version) {
        expression (version)
      }
      param(skillset) {
        expression (skillset)
      }
      param (spells) {
        expression (spells)
      }
    }
  }
}